import { LocalizationType } from '../types';
import { enDefaults } from './en';
import { Resource } from 'i18next';

export const localizationDefaults: Resource = {
  en: { translation: enDefaults }
}
